{
  "name": "oracle-bridge",
  "version": "2.0.0",
  "description": "BrainSAIT Oracle OASIS Bridge Worker — appointments, labs, radiology, documents, claims",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
